You are helping me finish my AI trading website.

Current progress:

* The app has a React frontend and Flask/Python backend.
* The page is called `backtester.jsx`.
* The bot/backtester currently runs but mostly tests random crypto dates.
* The symbols/dashboard section currently only properly shows crypto.
* I want the website to support multiple markets: crypto, forex, indices, commodities, and stocks.
* Render backend deploy has worked before.
* Vercel frontend deploy failed before, so check frontend build errors carefully.
* After every code change, give me the push command.
* I need step-by-step instructions and only one step at a time when installing or moving files.
* Do not remove existing UI/features unless necessary.

Main goal:
Make the rest of the trading website work properly, especially:

1. Expand market/symbol support beyond crypto.
2. Make the backtester use real candle data for all supported markets.
3. Fix “not enough candle data” issues.
4. Keep dashboard symbols, backtester symbols, and backend bot config in sync.
5. Make the UI look professional and consistent with the trading platform design.
6. Make sure Render and Vercel deploy successfully.

Before editing, inspect the repo structure and identify:

* frontend folder
* backend folder
* API routes
* symbol config
* candle data fetching logic
* backtester logic
* deployment config
